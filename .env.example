# Copy to .env (do not commit).
# Machine-local values only. Runtime policy/defaults live in configs/runtime.toml.
# Required for LLM runtime: OPENAI_BASE_URL and OPENAI_MODEL.
OPENAI_API_KEY=
OPENAI_BASE_URL=http://127.0.0.1:20128/v1
OPENAI_MODEL=cx/gpt-5.4-mini
